export enum WrittenWishesStates {
  playersWritting = 'playersWritting',
  showingWishes = 'showingWishes',
}

export enum WrittenWishesInputNames {
  wishesInput = 'wishesInput',
  finishWritting = 'finishWritting',
}
